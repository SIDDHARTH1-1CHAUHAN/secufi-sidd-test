import { StaticPanelScreen } from '@/src/features/shared/StaticPanelScreen';

export default function NotificationsRoute() {
  return <StaticPanelScreen title='Notifications' subtitle='Household signals, agent updates, and upcoming deadlines.' />;
}
