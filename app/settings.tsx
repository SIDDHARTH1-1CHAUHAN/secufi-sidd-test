import { StaticPanelScreen } from '@/src/features/shared/StaticPanelScreen';

export default function SettingsRoute() {
  return <StaticPanelScreen title='Settings' subtitle='Preferences, privacy controls, and household access.' />;
}
