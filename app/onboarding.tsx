import { StaticPanelScreen } from '@/src/features/shared/StaticPanelScreen';

export default function OnboardingRoute() {
  return <StaticPanelScreen title='Onboarding' subtitle='Consent, household setup, and secure document permissions.' />;
}
